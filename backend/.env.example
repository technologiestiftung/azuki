# Optional: OpenRouter API key for AI-powered ranking & explanations
# Without it, results will use the pre-filter fallback
OPENROUTER_API_KEY=

# Optional: Default AI model (must be from the curated list in shared/src/models.ts)
# Falls back to first model in the list if not set
AI_MODEL=

# Optional: Password to protect the API
APP_PASSWORD=
